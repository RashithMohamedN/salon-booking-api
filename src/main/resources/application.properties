# ?? App Info (change per client)
spring.application.name=Salon Booking API

# ?? MySQL (change per client) ???????????????????
spring.datasource.url=jdbc:mysql://localhost:3306/salon_booking
spring.datasource.username=root
spring.datasource.password=Rabi17Rash
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? JPA ?????????????????????????????????????????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ?? Server ??????????????????????????????????????
server.port=8080

# ?? JWT (change secret per client) ??????????????
jwt.secret=salonBookingSecretKey2024MaduraiTamilNadu12345678
jwt.expiration=86400000

# ?? Business Config (change per client) ?????????
app.business.name=Glamour Salon
app.business.phone=9876543210
app.business.whatsapp=919876543210
app.business.address=No. 12, Anna Nagar, Madurai

# ?? CORS ? add client domain here ???????????????
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000